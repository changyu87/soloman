# Soloman 技能文档

## 1. 什么是 Soloman

Soloman 是一个自举的通用 AI 工作范式，它将复杂的 AI 辅助工作分解为专门的**角色**——一个**总控**（主会话，面向人类）编排**子代理**（规划器、构建器、审计器等），每个子代理拥有独立的上下文窗口。所有通信通过文件系统进行。

Soloman 可以管理任何项目——包括生产和改进自身。

## 2. Soloman 能做什么

- **复杂项目管理**：处理大型、多步骤的项目，避免单会话上下文过载
- **结构化工作流**：提供清晰的工作流程，从需求分析到执行和审查
- **角色专业化**：每个角色负责特定的任务，提高工作质量和效率
- **智能资源管理**：根据任务复杂度自动分配合适的 AI 资源
- **自我进化**：可以管理自身的开发和改进
- **多语言支持**：自动检测用户语言，所有产物使用中文编写

## 3. 核心角色

| 角色 | 职责 |
|------|------|
| **总控** | 人机接口、提示丰富化、编排、状态管理 |
| **规划器** | 生成详尽的执行计划 |
| **构建器** | 执行计划——编写代码、创建内容 |
| **审计器** | 独立审查（可选，默认启用） |
| **档案员** | 项目知识管理员——知道每个文件的位置 |
| **铁匠** | 创建/编辑/删除角色——自举引擎 |
| **军需官** | AI/硬件资源管理 |
| **蒸馏器** | 令牌效率审计 |

## 4. 工作流程

1. **用户提出请求**（可能不够清晰）
2. **总控**丰富化提示，必要时提出澄清问题
3. **档案员**（如果需要）提供相关文件路径
4. **规划器**生成执行计划
5. **审计器**（如果启用）审查计划
6. **用户批准计划**
7. **构建器**执行计划
8. **审计器**（如果启用）审查交付物
9. **总控**呈现结果，保存状态

## 5. 安装方法

### 5.1 从源代码安装

```bash
# 克隆仓库
cd ~/work
git clone https://github.com/changyu87/soloman.git
cd soloman

# 安装到默认位置 (~/.trae-cn/skills/soloman/)
./install.sh

# 或者安装到自定义位置
SOLOMAN_INSTALL_DIR=/path/to/install ./install.sh
```

### 5.2 验证安装

安装完成后，检查安装目录是否存在：

```bash
ls ~/.trae-cn/skills/soloman/
```

应该看到以下文件和目录：
- SKILL.md
- roles/
- protocols/
- templates/
- knowledge/
- VERSION

## 6. 调用和使用

### 6.1 在任何项目中使用

```bash
cd ~/work/my-project
# 在 Trae 会话中输入：
# /soloman
```

会话将转变为总控。它将：
1. 如果项目尚未初始化，主动提供搭建 `.soloman/` 目录
2. 呈现当前状态并询问你想处理什么

### 6.2 用于自我进化

```bash
cd ~/work/soloman
# 在 Trae 会话中输入：
# /soloman
```

会话检测到当前工作目录中存在 `SOLOMAN.md` 并进入**自身模式**。行为与普通项目相同，区别在于：铁匠 被允许将通用角色编辑写回源代码仓库。

## 7. 核心功能

### 7.1 需求丰富化

总控会自动分析用户请求，识别歧义、缺失的约束条件和隐含假设，并在必要时提出澄清问题。

### 7.2 智能规划

规划器会根据丰富化的需求生成详细的执行计划，包括：
- 具体的任务步骤
- 精确的文件路径
- 完整的代码或操作说明
- 验证步骤

### 7.3 独立审查

审计器会对计划和交付物进行独立审查，确保质量和一致性。

### 7.4 上下文管理

Soloman 会自动管理上下文窗口，当接近容量上限时，会启动优雅交接协议，保存状态并建议用户启动新会话。

### 7.5 资源优化

军需官会管理 AI 和硬件资源分配，根据任务复杂度推荐合适的资源。

## 8. 项目结构

```
soloman/               # Soloman 源代码仓库
├── SOLOMAN.md              # 快速入门指南、自模式标记
├── SOLOMAN_DOC.md          # 详细文档
├── skill/SKILL.md           # Trae 技能定义 + 总控行为规范
├── roles/                   # 通用角色定义
├── protocols/               # 交互协议
├── templates/               # 项目脚手架
├── knowledge/               # 随附参考（索引、不变式、约定）
├── install.sh / uninstall.sh
└── changelog.md             # 进化日志

~/.trae-cn/skills/soloman/   # 已安装的技能包（运行时只读）
├── SKILL.md
├── roles/ protocols/ templates/ knowledge/
└── VERSION

any-project/.soloman/       # 每个项目的控制平面（在源代码仓库中被 gitignore）
├── config.yaml              # 项目设置
├── state/                   # 会话状态（current.md、session-log.md、checkpoints/）
├── workstreams/             # 活跃和已完成的工作流
├── roles/                   # 项目特定的自定义角色
└── knowledge/               # 运行时项目知识
```

## 9. 元命令

Soloman 支持以下元命令：

- **"status"** / **"where are we?"** → 读取并呈现当前状态
- **"list roles"** → 列出可用角色
- **"toggle auditor"** → 切换审计器状态
- **"create a [X] role"** → 创建新角色
- **"edit [role] role"** → 编辑角色
- **"delete [role] role"** → 删除角色
- **"change resources"** → 重新评估资源分配
- **"clear"** → 清理状态
- **"distill"** → 运行令牌效率审计

## 10. 最佳实践

1. **明确的需求**：提供清晰、详细的需求描述，包括目标、约束和成功标准
2. **合理的范围**：将大型项目分解为多个小工作流
3. **定期保存**：当收到上下文溢出警告时，及时启动新会话
4. **利用审计**：启用审计器以获得独立的质量审查
5. **资源管理**：根据任务复杂度选择合适的资源
6. **版本控制**：将 `.soloman/` 目录添加到 `.gitignore` 文件中

## 11. 故障排除

### 11.1 安装问题

- **权限错误**：确保你有安装目录的写入权限
- **依赖缺失**：确保 git 已安装（用于安装 writing-plans 技能）

### 11.2 运行问题

- **上下文溢出**：当收到警告时，启动新会话并输入 `/soloman` 恢复
- **角色未找到**：确保安装完整，所有角色文件都存在
- **计划审批问题**：确保提供明确的自由文本批准，而不是简单的"是"或"好"

## 12. 版本历史

- **v0.3.0**：所有角色名称改为中文
- **v0.2.0**：所有产物改为中文，角色"Interfacer"改名为"总控"
- **v0.1.0**：项目从 the-paradigm 重命名为 soloman

## 13. 总结

Soloman 是一个强大的 AI 工作范式，通过角色专业化和结构化工作流程，解决了单会话 AI 工作的上下文过载问题。它不仅可以管理复杂的项目，还可以自我进化和改进。

通过遵循本文档的安装和使用指南，你可以充分利用 Soloman 的能力，提高 AI 辅助工作的效率和质量。
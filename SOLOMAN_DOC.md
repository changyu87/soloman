# Soloman 技能文档

## 1. 什么是 Soloman

Soloman 是一种专为个人用户设计的自举式通用 AI 工作范式，通过将复杂的 AI 辅助工作分解为专业化的**角色**（Roles）体系，实现高效的项目管理与执行。核心架构由一个**总控**（Interfacer，主会话，面向人类）和多个**子代理**（Subagents，如规划器、构建器、审计器等）组成，每个子代理拥有独立的上下文窗口，所有通信通过文件系统进行持久化。

Soloman 旨在帮助个人用户以工程化的标准流程执行各类项目，包括管理和演进 Soloman 自身，从而实现更高效、更可靠的 AI 辅助工作。

## 2. Soloman 核心能力

- **复杂项目管理**：处理大型、多步骤的项目，有效避免单会话上下文过载问题
- **结构化工作流**：提供标准化的工作流程，涵盖从需求分析、规划到执行和审查的完整生命周期
- **角色专业化**：通过职责分离实现专业化分工，提高工作质量和执行效率
- **智能资源管理**：根据任务复杂度自动分配适配的 AI 资源
- **自举进化**：具备自我管理和持续改进能力，支持自身的迭代与演进
- **多语言支持**：自动检测用户语言，所有产物以中文编写，确保一致性

## 3. 核心角色

| 角色（英文） | 角色（中文） | 职责 |
|-------------|-------------|------|
| **Interfacer** | 总控 | 人机接口、需求丰富化、工作流编排、状态管理 |
| **Planner** | 规划器 | 生成详尽的执行计划，包括任务分解、资源分配和时间线 |
| **Builder** | 构建器 | 执行计划——编写代码、创建内容、实现具体功能 |
| **Auditor** | 审计器 | 独立审查计划和交付物，确保质量和一致性（可选，默认启用） |
| **Archivist** | 档案员 | 项目知识管理，维护文件索引和项目上下文 |
| **Forge** | 铁匠 | 创建/编辑/删除角色，作为系统的自举引擎 |
| **Quartermaster** | 军需官 | AI/硬件资源管理和优化 |
| **Distiller** | 蒸馏器 | 令牌效率审计，优化系统性能 |

## 4. 工作流程

Soloman 采用标准化的工作流程，确保项目从需求到交付的全过程可追溯、可管理：

1. **需求提交**：用户提出项目请求（可能不够详细或明确）
2. **需求丰富化**：总控（Interfacer）分析请求，识别歧义、缺失的约束条件和隐含假设，必要时提出澄清问题
3. **上下文收集**：档案员（Archivist）根据需要提供相关文件路径和项目上下文
4. **计划生成**：规划器（Planner）基于丰富化的需求生成详细的执行计划
5. **计划审查**：审计器（Auditor）对计划进行独立审查，确保可行性和质量（如启用）
6. **计划批准**：用户对计划进行明确的自由文本批准
7. **计划执行**：构建器（Builder）按照批准的计划执行具体任务
8. **交付物审查**：审计器（Auditor）对执行结果进行独立审查（如启用）
9. **结果呈现**：总控（Interfacer）向用户呈现最终结果，并保存项目状态

## 5. 安装方法

### 5.1 从源代码安装

Soloman 提供了简单的安装脚本，支持标准安装和自定义安装路径：

```bash
# 克隆仓库
cd ~/work
git clone https://github.com/changyu87/soloman.git
cd soloman

# 标准安装（默认路径：~/.trae-cn/skills/soloman/）
./install.sh

# 自定义安装路径
SOLOMAN_INSTALL_DIR=/path/to/custom/location ./install.sh
```

### 5.2 安装验证

安装完成后，可通过以下命令验证安装是否成功：

```bash
ls ~/.trae-cn/skills/soloman/
```

成功安装后应看到以下文件和目录结构：
- SKILL.md         # Soloman 技能定义文件
- roles/           # 角色定义目录
- protocols/       # 工作流协议目录
- templates/       # 项目模板目录
- knowledge/       # 知识库目录
- VERSION          # 版本标识文件

## 6. 调用和使用

### 6.1 在普通项目中使用

Soloman 可以在任何项目目录中启动，自动初始化并管理项目：

```bash
# 进入项目目录
cd ~/work/my-project

# 在 Trae 会话中激活 Soloman
# /soloman
```

激活后，总控（Interfacer）将：
1. 检查项目是否已初始化，如未初始化则自动提供 `.soloman/` 目录搭建
2. 呈现当前项目状态
3. 询问用户希望处理的具体任务

### 6.2 自我进化模式

当在 Soloman 源代码仓库中启动时，系统会自动进入**自身模式**（Self-mode）：

```bash
# 进入 Soloman 源代码目录
cd ~/work/soloman

# 在 Trae 会话中激活 Soloman
# /soloman
```

在自身模式下，系统行为与普通项目相同，但具有额外权限：铁匠（Forge）角色被允许将通用角色定义的编辑写回源代码仓库，实现 Soloman 的自我进化和迭代改进。

## 7. 核心功能

### 7.1 需求丰富化

总控（Interfacer）会对用户提出的初始请求进行深度分析，自动识别潜在的歧义、缺失的约束条件和隐含假设，并通过结构化的澄清流程确保需求的完整性和准确性。

### 7.2 智能规划

规划器（Planner）基于丰富化的需求生成详尽的执行计划，包括：
- 精细化的任务分解和时间线
- 精确的文件路径和修改点
- 完整的代码实现或操作说明
- 验证步骤和质量检查点

### 7.3 独立审查

审计器（Auditor）对计划和交付物进行独立审查，确保方案的可行性、质量一致性和符合项目目标。审计过程包括风险评估和改进建议。

### 7.4 上下文管理

Soloman 实现了智能上下文窗口管理，当检测到上下文接近容量上限时，会启动优雅交接协议，保存当前状态并引导用户启动新会话以保持系统性能。

### 7.5 资源优化

军需官（Quartermaster）负责 AI 和硬件资源的智能分配，根据任务复杂度和项目需求推荐最优资源配置，平衡性能和成本。

## 8. 技术特性

Soloman 采用一系列先进的技术设计，确保系统的可靠性、效率和可扩展性：

- **并行子代理调度**：总控（Interfacer）支持并行调度多个子代理，显著提高复杂任务的执行效率
- **纯文本通信机制**：所有角色间通过文件系统进行纯文本通信，确保通信内容的可追溯性、一致性和可审计性
- **独立上下文窗口**：每个子代理拥有独立的上下文窗口，避免上下文污染，提高系统稳定性
- **自举进化能力**：系统具备自我管理和持续改进能力，支持自身的迭代与演进
- **文件系统持久化**：所有状态和通信通过文件系统持久化，确保数据可靠性和系统可恢复性
- **分层状态管理**：总控（Interfacer）负责轻量级状态管理，档案员（Archivist）在里程碑处负责重量级状态管理
- **智能上下文管理**：自动检测上下文窗口使用情况，在接近容量上限时执行优雅交接，保持系统性能
- **多语言交互支持**：自动检测用户语言，所有产物以中文编写，确保一致性和可理解性

## 9. 项目结构

Soloman 采用清晰的分层结构，确保代码组织的逻辑性和可维护性：

### 9.1 源代码仓库结构

```
soloman/               # Soloman 源代码仓库
├── SOLOMAN.md              # 快速入门指南、自身模式标记
├── SOLOMAN_DOC.md          # 详细技术文档
├── skill/SKILL.md           # Trae 技能定义 + 总控行为规范
├── roles/                   # 通用角色定义
├── protocols/               # 工作流和交互协议
├── templates/               # 项目初始化脚手架
├── knowledge/               # 随附参考资料（索引、不变式、约定）
├── install.sh               # 安装脚本
├── uninstall.sh             # 卸载脚本
└── changelog.md             # 版本变更记录
```

### 9.2 已安装技能包结构

```
~/.trae-cn/skills/soloman/   # 已安装的技能包（运行时只读）
├── SKILL.md                 # 技能定义文件
├── roles/                   # 角色定义目录
├── protocols/               # 协议目录
├── templates/               # 模板目录
├── knowledge/               # 知识库目录
└── VERSION                  # 版本标识文件
```

### 9.3 项目控制平面结构

```
any-project/.soloman/       # 每个项目的控制平面（建议在 gitignore 中排除）
├── config.yaml              # 项目配置文件
├── state/                   # 会话状态
│   ├── current.md           # 当前状态
│   ├── session-log.md        # 会话日志
│   └── checkpoints/         # 状态检查点
├── workstreams/             # 工作流管理
│   ├── active/              # 活跃工作流
│   └── completed/           # 已完成工作流
├── roles/                   # 项目特定的自定义角色
└── knowledge/               # 运行时项目知识库
```

## 10. 元命令

Soloman 提供了一系列元命令，用于系统管理和状态控制：

- **"status"** / **"where are we?"** → 读取并呈现当前项目状态
- **"list roles"** → 列出所有可用角色及其描述
- **"toggle auditor"** → 切换审计器（Auditor）的启用/禁用状态
- **"create a [X] role"** → 创建新的自定义角色
- **"edit [role] role"** → 编辑现有角色的定义
- **"delete [role] role"** → 删除指定的自定义角色
- **"change resources"** → 重新评估并优化资源分配
- **"clear"** → 清理项目状态（支持不同层级的清理）
- **"distill"** → 运行令牌效率审计，优化系统性能

## 11. 最佳实践

为充分发挥 Soloman 的能力，建议遵循以下最佳实践：

1. **明确的需求定义**：提供清晰、详细的需求描述，包括具体目标、约束条件和成功标准
2. **合理的项目范围**：将大型项目分解为多个小工作流，每个工作流专注于特定的功能模块
3. **及时的状态管理**：当收到上下文溢出警告时，及时启动新会话以保持系统性能
4. **有效的质量控制**：启用审计器（Auditor）以获得独立的质量审查，确保交付物符合标准
5. **智能的资源分配**：根据任务复杂度和项目需求选择合适的 AI 资源
6. **规范的版本控制**：将 `.soloman/` 目录添加到 `.gitignore` 文件中，避免将运行时状态纳入版本控制

## 12. 故障排除

### 12.1 安装问题

| 问题 | 可能原因 | 解决方案 |
|------|----------|----------|
| **权限错误** | 安装目录写入权限不足 | 确保当前用户对目标安装目录拥有写入权限 |
| **依赖缺失** | git 未安装 | 安装 git 后重新运行安装脚本 |
| **安装失败** | 网络连接问题 | 检查网络连接后重试安装 |

### 12.2 运行问题

| 问题 | 可能原因 | 解决方案 |
|------|----------|----------|
| **上下文溢出** | 会话上下文达到容量上限 | 启动新会话并输入 `/soloman` 恢复工作 |
| **角色未找到** | 安装不完整或文件损坏 | 重新运行安装脚本确保所有角色文件存在 |
| **计划审批问题** | 批准方式不符合要求 | 提供明确的自由文本批准，详细说明批准的计划内容 |
| **资源分配错误** | 硬件或 AI 资源配置不当 | 使用 `change resources` 命令重新评估资源分配 |

## 13. 总结

Soloman 是一种创新的 AI 工作范式，通过角色专业化分工和标准化工作流程，有效解决了单会话 AI 工作的上下文过载问题。它为个人用户提供了一套工程化的项目执行框架，不仅能够管理复杂的项目，还具备自我进化和持续改进的能力。

Soloman 的核心优势在于：
- **结构化工作流**：提供从需求分析到交付的完整流程，确保项目执行的一致性和可追溯性
- **角色专业化**：通过明确的角色分工，提高工作质量和执行效率
- **技术创新**：采用并行子代理调度、纯文本通信和独立上下文窗口等先进设计
- **自举能力**：支持自身的迭代与演进，持续优化系统性能

通过遵循本文档的安装和使用指南，个人用户可以充分利用 Soloman 的能力，显著提高 AI 辅助工作的效率和质量，实现更加专业化、标准化的项目管理流程。